import { GalleryPageContent } from "@/components/gallery/GalleryPageContent";

export const revalidate = 0;

export default function BrandingWorkPage() {
  return <GalleryPageContent category="branding-work" />;
}
